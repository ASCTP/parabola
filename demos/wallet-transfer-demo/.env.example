# Optional: initial network the demo starts on. "mainnet" or "testnet".
# Falls back to "testnet" when unset, so a local checkout never starts on mainnet by default.
# Vite only exposes env vars prefixed with VITE_ to client code.
VITE_NETWORK=testnet
